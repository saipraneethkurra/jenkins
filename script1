pipeline {
    agent any
    stages {
        stage('Clone Repository') {
            steps {
                script {
                    sh 'git clone git@github.com:saipraneethkurra/jenkins.git'
                }
            }
        }
    }
}
